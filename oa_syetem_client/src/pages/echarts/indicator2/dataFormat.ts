export type CensusItem = {
  value: number;
  label: string;
  color: string;
};

export type SuperviseNode = {
  days: number;
  comStatus: string;
  taskName: string;
  finishPlandate: string;
  user: string;
};
