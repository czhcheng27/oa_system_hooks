import React, {
  useState,
  useEffect,
  useCallback,
  useRef,
  useMemo,
} from "react";
import css from "./index.module.less";

const Admin = (props) => {
  return <div>Admin</div>;
};

export default Admin;
