import React, {
  useState,
  useEffect,
  useCallback,
  useRef,
  useMemo,
} from "react";
import css from "./index.module.less";

const InfoDetail = (props) => {
  return <div className={css.box_wrap}>InfoDetail</div>;
};

export default InfoDetail;
