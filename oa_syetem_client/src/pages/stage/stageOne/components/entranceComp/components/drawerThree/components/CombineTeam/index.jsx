import React, {
  useState,
  useEffect,
  useCallback,
  useRef,
  useMemo,
} from "react";
import css from "./index.module.less";

const CombineTeam = (props) => {
  return <div>CombineTeam</div>;
};

export default CombineTeam;
