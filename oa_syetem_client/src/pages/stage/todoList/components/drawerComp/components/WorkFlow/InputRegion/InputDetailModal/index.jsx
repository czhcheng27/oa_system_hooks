import React, { useState, useEffect, useRef } from "react";
import { Modal } from "antd";
import css from "./index.module.less";

const InputDetailModal = (props) => {
  return (
    <Modal open={true} width={1000} centered>
      asd
    </Modal>
  );
};

export default InputDetailModal;
