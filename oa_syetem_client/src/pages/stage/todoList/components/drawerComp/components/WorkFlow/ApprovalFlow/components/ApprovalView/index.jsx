import React, {
  useState,
  useEffect,
  useCallback,
  useRef,
  useMemo,
} from "react";
import css from "./index.module.less";

const ApprovalView = (props) => {
  return <div className={css.appr_view}>ApprovalView</div>;
};

export default ApprovalView;
