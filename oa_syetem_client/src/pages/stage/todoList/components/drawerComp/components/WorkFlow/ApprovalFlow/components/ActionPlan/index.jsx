import React, {
  useState,
  useEffect,
  useCallback,
  useRef,
  useMemo,
} from "react";
import css from "./index.module.less";

const ActionPlan = (props) => {
  return <div className={css.act_plan}>ActionPlan</div>;
};

export default ActionPlan;
